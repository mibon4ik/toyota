
/**
 * Represents a vehicle with its basic details.
 */
export interface Vehicle {
  /** The Vehicle Identification Number (unique). */
  vin: string;
  /** The make of the vehicle (e.g., Toyota). */
  make: string;
  /** The model of the vehicle (e.g., Camry). */
  model: string;
}
